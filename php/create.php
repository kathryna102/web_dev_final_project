<?php 
// Create
//establishes the database connection
include "db.php";

// TODO: prepare data for insertion
// use SQL INSERT query to insert the new task into the database
// - tasks will have a name (string), category (string), due date (YYYY-MM-DD)
//close the connection
//use prepared statements to prevent SQL injection? EX: $stmt = mysqli_prepare($conn, "INSERT INTO tasks (task) VALUES (?)");

?>